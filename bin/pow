#!/usr/bin/env node

var pow = require("pow");
var path = require("path");

var root = process.argv[2] || path.join(process.env.HOME, ".pow");
var port = 20559;

process.title = "pow";
pow.run(port, root);
