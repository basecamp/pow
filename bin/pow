#!/usr/bin/env node

require.paths.push(__dirname + "/../vendor");

var CoffeeScript = require("coffee-script");
var pow = require("../src/pow");
var path = require("path");

pow.run(3000, process.argv[2] || path.join(process.env.HOME, ".pow"));

