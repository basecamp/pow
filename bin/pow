#!/usr/bin/env node

require.paths.unshift(__dirname + "/../src")

var CoffeeScript = require("coffee-script");
var pow = require("pow");
var path = require("path");

pow.run(20559, process.argv[2] || path.join(process.env.HOME, ".pow"));
